---
import Layout from "../layouts/Layout.astro";
import { Image } from "astro:assets";
import placeholder from "../assets/blog-placeholder-1.jpg";
---

<Layout
  title="Rahul — Engineering Blog"
  description="Backend engineering, system design, Java, databases and distributed systems."
>
  <main class="container">
    <div class="profile">
      <Image class="avatar" src={placeholder} alt="Logo" />
      <div class="profileText">
        <h2 class="name">Rahul Harlalka</h2>
        <div class="subRow">
          <text>About</text>
          <text>|</text>
          <text>Blog</text>
        </div>
      </div>
    </div>
    <div>
      <h2>Hello !!</h2>
      <p>I am Rahul , a backend engineer at Flipkart</p>
      <p>
        I am passionate about creating backend systems at scale and love reading
        about databases.Recently , I have been reading about clustered db
        indexes and algorithms used by Lucene to search free text.
      </p>
      <p>
        After work , you find me exploring parts of city or playing badminton.
      </p>
    </div>
  </main>
</Layout>

<style>
  .hero h1 {
    font-size: clamp(34px, 4vw, 52px);
    line-height: 1.12;
    margin: 8px 0 12px;
  }
  .kicker {
    color: var(--muted);
    margin: 0;
    font-weight: 600;
  }
  .sub {
    color: var(--muted);
    font-size: 18px;
    max-width: 70ch;
    margin: 0 0 18px;
  }

  .ctaRow {
    display: flex;
    gap: 12px;
    flex-wrap: wrap;
    margin-top: 12px;
  }
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 10px 14px;
    border-radius: 12px;
    border: 1px solid var(--border);
    background: transparent;
    color: var(--text);
    font-weight: 600;
  }
  .btn.primary {
    background: rgba(96, 165, 250, 0.16);
    border-color: rgba(96, 165, 250, 0.25);
    color: var(--accent);
  }

  .metaRow {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin-top: 18px;
  }
  .pill {
    font-size: 13px;
    color: var(--muted);
    border: 1px solid var(--border);
    padding: 6px 10px;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.03);
  }

  .grid {
    margin-top: 18px;
    display: grid;
    gap: 16px;
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .links {
    display: flex;
    flex-wrap: wrap;
    gap: 14px;
  }
  .links a {
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 10px 12px;
    background: rgba(255, 255, 255, 0.03);
  }

  .muted {
    color: var(--muted);
  }

  @media (max-width: 760px) {
    .grid {
      grid-template-columns: 1fr;
    }
  }
</style>
